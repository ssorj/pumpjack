<class name="sender" type="/proton/client/link">
  A channel for sending messages.  A sender contains an ordered
  sequence of deliveries.

  <group name="basic">
    <method name="wait">
      Block until the remote peer confirms the open operation.

      <input name="timeout" type="duration" optional="true"/>
      <output type="/proton/client/sender"/>
    </method>
  </group>

  <group name="message-operations">
    <method name="send">
      Send a message on the link.

      **Blocking**

      This method blocks until the link has credit.

      <input type="message"/>
      <output type="task" item-type="tracker"/>
    </method>

    <method name="try-send">
      Send a message on the link.  If no credit is available, it
      returns null.

      <input type="message"/>
      <output type="task" item-type="tracker"/>
    </method>
  </group>
</class>
