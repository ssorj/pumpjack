<class name="endpoint">
  The base class for connection, session, and link.

  Every AMQP endpoint starts out in an uninitialized state and
  then proceeds linearly to an active and then closed state.
  This lifecycle occurs at both endpoints involved, and so the
  state model for an endpoint includes not only the known local
  state but also the last known state of the remote endpoint.

  The local and remote peers each maintain a set of
  corresponding local and remote endpoints.  Each peer evaluates
  and reconciles remote endpoint state changes as they are
  communicated over the wire.

  <link node="endpoint-options"/>
  <link node="connection"/>
  <link node="session"/>
  <link node="link"/>

  <link relation="amqp" href="http://docs.oasis-open.org/amqp/core/v1.0/os/amqp-core-transport-v1.0-os.html#doc-idp48736">Conceptual overview</link>
  
  <group name="endpoint-state-group">
    <property name="uninitialized" type="boolean" value="[discovered]" proposed="true">
      The local and remote endpoint states are UNINITIALIZED.
    </property>

    <property name="active" type="boolean" value="[discovered]" proposed="true">
      The local and remote endpoint states are ACTIVE.
    </property>

    <property name="closed" type="boolean" value="[discovered]" proposed="true">
      The local and remote endpoint states are CLOSED.
    </property>
    
    <property name="local-state" type="endpoint-state" value="UNINITIALIZED">
      Local endpoint state.
    </property>

    <property name="remote-state" type="endpoint-state" value="[discovered]">
      Remote endpoint state.
    </property>

    <property name="local-condition" type="condition" nullable="true">
      The error condition of the local peer.
    </property>
    
    <property name="remote-condition" type="condition" value="[discovered]" nullable="true">
      The error condition of the remote peer.
    </property>
    
    <property name="remote-properties" type="map" key-type="symbol" item-type="object" value="[discovered]" nullable="true">
      Application properties defined by the remote peer.
    </property>

    <property name="remote-offered-capabilities" type="array" item-type="symbol" value="[discovered]" nullable="true">
      Extensions the remote peer supports.
    </property>
    
    <property name="remote-desired-capabilities" type="array" item-type="symbol" value="[discovered]" nullable="true">
      Extensions the remote peer can use.
    </property>
  </group>
</class>

<enumeration name="endpoint-state">
  Endpoint state values.

  <value name="UNINITIALIZED">
    The endpoint is uninitialized.
  </value>

  <value name="ACTIVE">
    The endpoint is active.
  </value>

  <value name="CLOSED">
    The endpoint is closed.
  </value>
</enumeration>
